[project]
name = "docker-vdirsyncer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp-oauthlib>=0.1.0",
    "vdirsyncer>=0.19.3",
]
